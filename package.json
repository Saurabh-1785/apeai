{
  "name": "apeai",
  "version": "1.0.0",
  "description": "ApeAI Product Operations Platform",
  "scripts": {
    "build": "npm install --prefix backend && npm run --prefix backend build",
    "start": "npm run --prefix backend start"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.105.4"
  }
}
